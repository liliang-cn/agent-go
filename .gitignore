bin/

agentgo.toml

.DS_Store

.agentgo/vendor/
delegation/
*.db

data/
memories/

# UI
ui/node_modules/
ui/dist/
cmd/agentgo-ui/dist/
ui-config.json

workspace/

progress.md

findings.md

task_plan.md

# Local logs
.playwright-mcp/*.log
tmp/air-agentgo-ui/build-errors.log

# Eval harness output (keep eval/results/.gitkeep, ignore actual runs)
eval/results/*.json

# TS incremental build cache (regenerated every build)
*.tsbuildinfo

# superai example local data
.superai-data/

# superai web (Vite/AI SDK) deps
examples/superai/web/node_modules/

# Tool / build artifacts
.playwright-mcp/
.antigravitycli/
# Stray example binaries (from `go build ./examples/X` in repo root — use bin/)
/autonomous-agent
/superai
